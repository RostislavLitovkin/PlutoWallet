namespace PlutoWallet.Components.DAppConnectionView
{
    public enum DAppConnectionStateEnum
    {
        Waiting,
        Connecting,
        Confirming,
        Connected,

        Rejected,
        Disconnected,

        Reconnecting,
    }
}