<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PlutoWallet.Components.AssetSelect.UsdButtonView"
             WidthRequest="{Binding WidthRequest}">
    <Border BackgroundColor="{x:StaticResource PrimaryGray}"
        Stroke="{x:StaticResource PrimaryGray}">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="15" />

        </Border.StrokeShape>
        <HorizontalStackLayout AbsoluteLayout.LayoutBounds="0.5, 0.5, 1, 1"
                            AbsoluteLayout.LayoutFlags="All"
                            Spacing="5"
                            Padding="5"
                            HorizontalOptions="Center">
            <Label Text="USD"
                    TextColor="Black"
                    HorizontalTextAlignment="Center"
                    VerticalTextAlignment="Center"
                    HorizontalOptions="Center"
                   FontAttributes="Bold"
                />

        </HorizontalStackLayout>
    </Border>
</ContentView>
